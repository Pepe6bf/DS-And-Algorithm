# 씨름 선수

## 문제 설명
```
그리디 방식을 사용해서 풀었다.

문제 링크 : https://www.inflearn.com/course/%EC%9E%90%EB%B0%94-%EC%95%8C%EA%B3%A0%EB%A6%AC%EC%A6%98-%EB%AC%B8%EC%A0%9C%ED%92%80%EC%9D%B4-%EC%BD%94%ED%85%8C%EB%8C%80%EB%B9%84/dashboard
```

## 소스 코드
### Python
```python
import sys

input = sys.stdin.readline
N = int(input())
bodys = [list(map(int, input().split())) for _ in range(N)]
bodys.sort(key=lambda x:-x[0])

result = 0;
max = -1

for body in bodys:
    if (body[1] > max):
        max = body[1]
        result += 1

print(result)
```

### Java
```java
import java.util.*;
import java.io.*;

class Body implements Comparable<Body> {
    int h;
    int w;

    Body(int h, int w) {
        this.h = h;
        this.w = w;
    }

    @Override
    public int compareTo(Body o) {
        return o.h - this.h;
    }
}

public class Main {
    public static void main(String[] args) throws IOException {
        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
        StringTokenizer st;

        int N = Integer.parseInt(br.readLine());
        List<Body> arr = new ArrayList<>();

        for (int i = 0; i < N; i++) {
            st = new StringTokenizer(br.readLine());
            int h = Integer.parseInt(st.nextToken());
            int w = Integer.parseInt(st.nextToken());

            arr.add(new Body(h, w));
        }

        Collections.sort(arr);

        int result = 0;
        int max = Integer.MIN_VALUE;

        for (Body o : arr) {
            if (o.w > max) {
                max = o.w;
                result++;
            }
        }

        System.out.println(result);
    }
}
```